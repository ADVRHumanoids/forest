#!/usr/bin/expect -f

# get number of cores as arg since expect interpreter does not have nproc cmd
set nproc [lindex $argv 0]

spawn forest all -j$nproc --mode xeno --pwd
expect "Password: "
send "user\r"
# interact
wait
